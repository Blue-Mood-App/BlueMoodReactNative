import React, { useEffect, useState } from "react";
import { useSelector, useDispatch } from "react-redux";
import { Text } from "react-native";
import { setUpdatedConnect } from "../store/registration";

const UpdatedMeet = ({ navigation }) => {
  //   const user = useSelector((state) => state.auth);
  //   const { id: userId } = user;
  //   const dispatch = useDispatch();
  //   const agreedToMeet = useSelector(state);
  //   console.log(agreedToMeet);
  //   const { id: userId } = user;
  //   const [agreedToMeet, setAgreedToMeet] = useState(false);
  return <Text>Hi</Text>;
};

export default UpdatedMeet;
